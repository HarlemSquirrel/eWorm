
  <p>
    <%= link_to review.user.username, user_path(review.user) %>
     rated <em><%= link_to review.book.title, book_path(review.book) %></em>
      <%= review.rating %> out of 5<br>
    "<%= review.content %>"
  </p>
